<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray"
    tools:context="com.example.myapp.editnoteactivity">

    <!-- Toolbar pour le titre de la note -->
    <androidx.appcompat.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:id="@+id/toolbarofeditnote"
        android:background="@color/white">

        <!-- EditText pour le titre de la note -->
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="12dp"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:id="@+id/edittitleofnote"/>
    </androidx.appcompat.widget.Toolbar>

    <!-- EditText pour le contenu de la note -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:background="@color/lighter_gray"
        android:gravity="top|left"
        android:textColor="@color/black"
        android:layout_below="@id/toolbarofeditnote"
        android:padding="15dp"
        android:id="@+id/editcontentofnote"/>

    <!-- FloatingActionButton pour enregistrer les modifications de la note -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="30dp"
        android:layout_marginEnd="30dp"
        android:id="@+id/saveeditnote"
        android:src="@drawable/ic_baseline_save_24"
        android:background="@color/teal"
        app:maxImageSize="40dp"/>
</RelativeLayout>
